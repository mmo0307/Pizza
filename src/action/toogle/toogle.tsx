import { createAction } from "@reduxjs/toolkit";

export const accountToogle = createAction("accountToogle");

export const ordersToogle = createAction("ordersToogle");

export const shopToogle = createAction("shopToogle");

export const menuToogle = createAction("menuToogle");
